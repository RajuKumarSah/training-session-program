#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
	char ch[100];
     scanf("%[^\n]s",ch);
    int i,c=0;
    for(i=0;i<strlen(ch);i++)
    {
        if(ch[i]==' ')
        {
            c++;
        }
    }
    printf("%d",c+1);
	return 0;
}
