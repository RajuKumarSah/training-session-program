#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
    long long int n;
    scanf("%lld",&n);
    long long int c=0,i;
    for(i=2;i<=n;i++)
    {
        if(n%i==0)
        {
            c++;
        }
    }
    printf("%lld",c);
    return 0;
}
