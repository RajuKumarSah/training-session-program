#include<stdio.h>
#include<string.h>
int strlen1(char *s)
{
    int i=0;
    while(s[i++])
    {
        
    }
    return --i;
}
int main()
{
	char str[50];
	gets(str);
	printf("%d", strlen1(str));
	return 0;
}
