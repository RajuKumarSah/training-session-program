#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

  int n;
    scanf("%d",&n);
    while(n--)
    {
        int k;
        scanf("%d",&k);
        for(int i=1;i<=k+1;i++)
        {
            if(k%i==0)
            {
                printf("%d ",i);
            }
        }
            printf("\n");
    }
    return 0;
}
