#include<stdio.h>
#include<math.h>
double hypotenuse(double base, double perp)
{
	double t;
    t=sqrt(base*base+perp*perp);
        return t;
}
int main()
{
	double A, B, C;
	scanf("%lf%lf", &A, &B);
	C = hypotenuse(A, B);
	printf("%.6lf", C);
	return 0;
}
