#include <stdio.h>

int main()
{
	int number;
	scanf("%d", &number);
    int i,c=0;
    for(i=1;i<=number;i++)
    {
        if(number%i==0)
        {
            c++;
        }
    }
    if(c==2)
        printf("yes\n");
    else
    {
        printf("no\n");
    }
	return 0;
}
