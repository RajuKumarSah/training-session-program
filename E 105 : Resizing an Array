#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n;
    scanf("%d",&n);
        int ch[n],i;
        for(i=0;i<n;i++)
        {
            scanf("%d",&ch[i]);
        }
        for(i=n-1;i>=0;i--)
        {
            printf("%d ",ch[i]);
        }
	
	return 0;
}
