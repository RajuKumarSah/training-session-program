#include <stdio.h>
int main()
{
	int var1, var2;
	scanf("%d %d", &var1, &var2);
    int sum1;
	sum1=var1+var2;
	//The following line will give an error because the variable sum won't be found
	printf("%d", sum1);
	return 0;
}
