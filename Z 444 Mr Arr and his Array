#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,a;
    scanf("%d%d",&n,&a);
    int b[n],i;
    for(i=0;i<n;i++)
    {
        scanf("%d",&b[i]);
    }
    for(i=a;i<n;i++)
    {
        printf("%d ",b[i]);
    }
    for(i=0;i<a;i++)
    {
        printf("%d ",b[i]);
    }
    
    return 0;
}
